
import open from './modules/open'
import hit from './modules/hit'

export default {
  deck: function(deck) {
    deck.blackjack = {}
    open.deck(deck);
    hit.deck(deck);
  },
  card: function(card) {
    card.blackjack = {};
    open.card(card);
    hit.card(card);
  }
}
