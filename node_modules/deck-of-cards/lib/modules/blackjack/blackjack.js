
import open from './modules/open'

export default {
  deck: function(deck) {
    deck.blackjack = {}
    open.deck(deck);
  },
  card: function(card) {
    card.blackjack = {};
    open.card(card);
  }
}
